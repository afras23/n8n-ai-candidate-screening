"""
Application-wide constants that are not environment-specific.

Thresholds that may change per environment belong in Settings instead.
"""

API_V1_PREFIX: str = "/api/v1"
CORRELATION_ID_HEADER: str = "X-Correlation-ID"
