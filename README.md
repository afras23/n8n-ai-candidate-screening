# n8n AI Candidate Screening

FastAPI service for CV parsing, AI scoring, job matching, and screening orchestration.

> **n8n workflow canvas screenshot** to be added after deploying the workflow. See [docs/n8n-workflow.md](docs/n8n-workflow.md) for the full setup guide.

## How to Run

See `.env.example` and use `docker compose up` from the repository root (see `Makefile` for `dev`, `test`, `lint`, and other targets).
